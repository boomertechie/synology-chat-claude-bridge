{
  "name": "synology-chat-executor",
  "version": "1.0.0",
  "description": "Claude Code Executor for Synology Chat integration",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
