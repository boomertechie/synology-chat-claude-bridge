# Synology Chat Executor Configuration

# Auth token - REQUIRED for production
# Generate with: openssl rand -hex 32
BRIDGE_AUTH_TOKEN=

# PAI directory (default: ~/.claude)
PAI_DIR=$HOME/.claude

# Claude CLI path (default: claude from PATH)
CLAUDE_CLI_PATH=claude

# Server port (default: 3457)
PORT=3457

# Log level (debug, info, warn, error)
LOG_LEVEL=info
